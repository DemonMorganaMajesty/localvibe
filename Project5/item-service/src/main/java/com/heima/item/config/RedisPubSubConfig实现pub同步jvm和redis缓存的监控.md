表格
操作方式	是否经过你的 Controller	是否发 Redis 消息	缓存是否同步
Postman/前端调用 API	✅ 是	✅ 是	✅ 同步
IDEA 直接改 MySQL	❌ 否	❌ 否	❌ 不同步
直接改数据库 = 绕过业务层 = 不会触发 convertAndSend()

所以需要修改 触发器+轮询+订阅消息+双写改造(优化 提高性能)

现在：                    改完后：
┌─────────┐              ┌─────────┐     ┌─────────────┐
│ 前端/API │ ──→          │ 前端/API │     │  IDEA直接改库  │
└────┬────┘              └────┬────┘     └──────┬──────┘
↓                        ↓                  ↓
┌─────────┐              ┌─────────┐     ┌─────────────┐
│Controller│              │Controller│     │ MySQL触发器   │
│convert  │              │convert   │     │ 写change_log  │
│AndSend()│              │AndSend() │     └──────┬──────┘
└────┬────┘              └────┬────┘            ↓
↓                        ↓           ┌─────────────┐
┌─────────┐              ┌─────────┐     │ 定时轮询任务  │
│Redis    │              │Redis    │     │ 同步缓存      │
│Pub/Sub  │              │Pub/Sub  │     └─────────────┘
└────┬────┘              └────┬────┘
↓                        ↓
┌─────────┐              ┌─────────┐
│各节点JVM │              │各节点JVM │
│缓存同步  │              │缓存同步  │
└─────────┘              └─────────┘



表格
方案	复杂度	实时性	稳定性	推荐度
触发器+轮询	低	5秒延迟	高	⭐⭐⭐⭐
Debezium	高	实时	高	⭐⭐⭐
云厂商 DTS	低	实时	极高	⭐⭐⭐⭐（花钱）
直接读 binlog	高	实时	中	⭐⭐
双写改造	低	实时	高	⭐⭐⭐⭐⭐
