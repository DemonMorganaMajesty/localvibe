Project4(hhmdp):8080:hmdp的前端nginx的端口 8081/8083:hmdp后端的负载均衡的端口

8082:redis哨兵集群使用的端口

wsl的ip:172.24.116.171



用户发起请求localhost:80/item/id ->客户端浏览器缓存

->windows的nginx(port 80(可能wallot占据)/81)服务器 提供静态的item.html文件 和反向代理 

->wsl的nginx(openresty port:8085,wsl和windows的端口是分开的可以相同,这里为了区别没有端口复用 ) 可以使用集群(但是要使得 每个id/统一请求发送到同一台wsl的nginx，实现的本地缓存nginx,不同机器的nginx本地缓存不能共享) nginx本地缓存速度比redis快但是内存少,后端重新启动就没有了,使用lua脚本实现数据写入本地缓存时候 也要写入redis时间可以redis设置的更长 现在我都设置的是30s

->redis缓存(可以实现集群 哨兵/哈希槽) stringRedistemplate 需要进行热预热处理 避免冷启动

->tomcat(java后端)的jvm进程缓存(caffine),后端的application 也可以开启多个的集群,实现负载均衡(端口不一样 8084 8086)

->缓存都没有查到,查数据库

缓存的同步问题：canal(不兼容)

这里使用的是redis的发收消息的订阅,redis需要改缓存的时候一起把jvm进程缓存一起改(实现 前端localhost:8084页面对数据的直接修改 /postman发get请求对改页面修改 的缓存同步) +增删改触发器+数据库修改的时间表(数据库新建一张表):后两部分是为了实现直接在数据库修改的缓存同步
