<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.localvibe.mapper.VoucherMapper">

    <select id="queryVoucherOfShop" resultType="com.localvibe.entity.Voucher" parameterType="java.lang.Long">
        SELECT
            v.`id`, v.`shop_id`, v.`title`, v.`sub_title`, v.`rules`, v.`pay_value`,
            v.`actual_value`, v.`type`, sv.`stock` , sv.begin_time , sv.end_time
        FROM tb_voucher v
        LEFT JOIN  tb_seckill_voucher sv ON v.id = sv.voucher_id
        WHERE v.shop_id = #{shopId} AND v.status = 1
    </select>

    <select id="queryMyVouchers" resultType="com.localvibe.entity.Voucher" parameterType="java.lang.Long">
        SELECT
            v.`id`, v.`shop_id`, v.`title`, v.`sub_title`, v.`rules`, v.`pay_value`,
            v.`actual_value`, v.`type`, sv.`stock`, sv.begin_time, sv.end_time,
            s.`name` AS shop_name, s.`images` AS shop_images, s.`area` AS shop_area,
            vo.`id` AS order_id, vo.`create_time` AS order_time
        FROM tb_voucher_order vo
        JOIN tb_voucher v ON vo.voucher_id = v.id
        LEFT JOIN tb_seckill_voucher sv ON v.id = sv.voucher_id
        LEFT JOIN tb_shop s ON v.shop_id = s.id
        WHERE vo.user_id = #{userId}
        ORDER BY vo.create_time DESC
    </select>
</mapper>
